import styled from 'styled-components'

export const TextContainer = styled.p`
  display: -webkit-box;
  text-align: center;

  overflow: hidden;
  max-height: 2.25rem;

  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
`
